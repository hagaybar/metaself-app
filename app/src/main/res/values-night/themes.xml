<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The dark half of the pair. See values/themes.xml for why this is a resource qualifier
         rather than a DayNight parent. -->
    <style name="Theme.MetaSelf" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
